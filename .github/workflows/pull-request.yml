name: Pull Request Validation

# 🎯 Purpose:
# Validates pull requests before they can be merged to master.
# Runs the same checks as master.yml to ensure code quality and prevent broken deployments.

on:
  pull_request:
    branches:
      - master
      - main
    types: [opened, synchronize, reopened]

  # Allow manual trigger from GitHub Actions UI
  workflow_dispatch:

# Cancel previous runs when new commits are pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Permissions for workflow operations
permissions:
  contents: read # Read repository contents
  pull-requests: write # Write PR comments

jobs:
  # ✅ All validation checks in reusable workflow
  # Runs lint, unit-tests, e2e-tests, and verify-build
  # All tests run in parallel for optimal speed (~15 min faster)
  validate:
    name: 🔍 Run All Validation Checks
    uses: ./.github/workflows/validate-code.yml
    secrets: inherit
    with:
      artifact-prefix: pr-${{ github.event.pull_request.number }}
      retention-days-coverage: 14
      retention-days-e2e-report: 14
      retention-days-e2e-results: 7
      upload-build-artifact: false
      validate-e2e-secrets: true

  # 📊 PR Status Summary with Comment
  # Provides a clear overview and posts automated comment
  status-comment:
    name: 📊 PR Status & Comment
    runs-on: ubuntu-latest
    needs: validate
    if: always() && github.event_name == 'pull_request'

    steps:
      - name: Post PR comment
        uses: actions/github-script@v8
        with:
          script: |
            const allChecksPassed = '${{ needs.validate.outputs.all-checks-passed }}' === 'success';
            const statusEmoji = allChecksPassed ? '✅' : '❌';
            const statusMessage = allChecksPassed 
              ? 'All checks passed! PR is ready to merge.'
              : 'PR validation failed - fix issues before merging';

            const comment = `## ${statusEmoji} Pull Request Validation Results

            **Overall Status:** ${statusMessage}

            ### Details

            - **Workflow Run:** [View Details #${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
            - **Commit:** \`${{ github.event.pull_request.head.sha }}\`
            - **Branch:** \`${{ github.head_ref }}\`

            ${allChecksPassed 
              ? '🎉 **Great work!** All validation checks passed successfully. This PR is ready to merge!' 
              : '⚠️  **Action Required:** Please review and fix the failing checks above before merging.'}

            ---

            <sub>🤖 Automated comment by [CityFlow CI/CD](${{ github.server_url }}/${{ github.repository }}/actions)</sub>
            `;

            // Find existing comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes('Pull Request Validation Results')
            );

            // Update or create comment
            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: comment
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: comment
              });
            }
